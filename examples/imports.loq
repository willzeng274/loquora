from core.utils import ( format, "stringify" );
import my.module.path;

export("foo", bar);

tool bar() -> String {
    message = "This is the bar function";
    print(message);
    return message;
}

result = bar();
print("Import/export example completed");
