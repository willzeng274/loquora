export tool concat(a: string, b: string) -> string {
    return a + b;
}

export tool repeat(s: string, n: int) -> string {
    result = "";
    i = 0;
    loop {
        if i >= n {
            break;
        }
        result = result + s;
        i = i + 1;
    }
    return result;
}
